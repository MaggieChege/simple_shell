#include "shell.h"

/**
 *exit_shell - Exits the shell with exit status specified
 *
 *@datash: SHell data needed to exit
 *Return: An integer value to specify success
 */
int exit_shell(data_shell *datash)
{
	char *str = "Work still in progress";

	(void)datash;
	printf("%s", str);
	return (0);
}
